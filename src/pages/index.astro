---
import BaseLayout from '../layouts/BaseLayout.astro';
import FaqList from '../components/FaqList.astro';
import LeadForm from '../components/LeadForm.astro';
import Icon from '../components/Icon.astro';
import type { IconName } from '../components/Icon.astro';
import { siteConfig, homepageServices, allServices, telHref, serviceCtaLabel } from '../config/site';
import { faqPage, originFromSite, plumberContractorNode, websiteNode, withContext, offerCatalogNode } from '../lib/schema';

const phone = telHref();
const origin = originFromSite(Astro.site);
const title = 'Virginia Beach Plumber | 24/7 Emergency, Drains & Water Heaters';
const description =
  `Local plumber in Virginia Beach for clogged drains, water heaters, leaks, sewer work, fixture repairs, and 24/7 emergency plumbing. Call ${siteConfig.phoneDisplay} or request service.`;

const orgSchema = withContext({
  ...plumberContractorNode(origin),
  description,
  hasOfferCatalog: offerCatalogNode(origin),
});

const websiteSchema = withContext(websiteNode(origin));

const webPageSchema = withContext({
  '@type': 'WebPage',
  '@id': `${origin}/#webpage`,
  url: `${origin}/`,
  name: title,
  description,
  inLanguage: 'en-US',
  isPartOf: { '@id': `${origin}/#website` },
  about: { '@id': `${origin}/#plumbing-contractor` },
  primaryImageOfPage: {
    '@type': 'ImageObject',
    url: `${origin}/images/hero-plumbing.jpg`,
  },
});

const featured: Array<(typeof homepageServices)[number] & { icon: IconName }> = [
  { ...homepageServices[0], icon: 'emergency' },
  { ...homepageServices[1], icon: 'drain' },
  { ...homepageServices[2], icon: 'heater' },
  { ...homepageServices[3], icon: 'sewer' },
  { ...homepageServices[4], icon: 'leak' },
  { ...homepageServices[5], icon: 'repair' },
];

const principles = [
  {
    title: 'Plain explanations',
    copy: 'You hear what we are looking at, what we recommend, and why, without jargon for its own sake.',
  },
  {
    title: 'Start with the problem',
    copy: 'We begin with the leak, the clog, the heater, or the backup in front of us, then decide the next useful step.',
  },
  {
    title: 'Homes and businesses',
    copy: 'Homeowners, landlords, restaurants, offices, hospitality sites, and property managers can all request help.',
  },
  {
    title: 'Virginia Beach coverage',
    copy: 'We serve Virginia Beach. Confirm your ZIP when you call or send a request.',
  },
];

const steps = [
  {
    num: '01',
    title: 'Tell Us What’s Wrong',
    copy: 'Call or send a short request with your ZIP and the plumbing problem you are dealing with.',
  },
  {
    num: '02',
    title: 'We Review the Details',
    copy: 'We confirm the service type and whether the job is in our Virginia Beach coverage area.',
  },
  {
    num: '03',
    title: 'Get Clear Next Steps',
    copy: 'You hear back about scheduling or, when it is urgent, how to reach us right away.',
  },
];

const moreServices = allServices.filter((s) => !homepageServices.some((h) => h.href === s.href));

const homeGuides = [
  {
    href: '/resources/emergency-plumbing-checklist/',
    category: 'Emergency',
    title: 'Emergency plumbing checklist',
    copy: 'Shutoff, safety, and when to call versus waiting.',
  },
  {
    href: '/resources/no-hot-water/',
    category: 'Water',
    title: 'No hot water',
    copy: 'What to check on a tank or tankless unit before you call.',
  },
  {
    href: '/resources/water-heater-leaking/',
    category: 'Water',
    title: 'Water heater leaking',
    copy: 'Tank body vs fitting drips, and when to shut the unit off.',
  },
  {
    href: '/resources/foul-drain-odor/',
    category: 'Drains',
    title: 'Foul drain smell',
    copy: 'Why a drain smells like sewage, and when odor means a larger problem.',
  },
  {
    href: '/resources/recurring-drain-clogs/',
    category: 'Drains',
    title: 'Recurring drain clogs',
    copy: 'Why the same drain keeps clogging, and when snaking is not enough.',
  },
];

const faqs = [
  {
    question: 'Are you a plumbing company or contractor in Virginia Beach?',
    answer: `Yes. Virginia Beach Plumbing is a local plumbing company and contractor for homes and businesses in Virginia Beach. Learn more on the <a href="/about/">about</a> page or browse <a href="/plumbing-services/">plumbing services</a>.`,
  },
  {
    question: 'Are you a 24-hour plumber in Virginia Beach?',
    answer: `Yes. We are open 24/7 for emergency plumbing: burst pipes, major leaks, and sewer backups. Call <a href="${phone}">${siteConfig.phoneDisplay}</a> or see the <a href="/emergency-plumber/">emergency plumbing</a> page.`,
  },
  {
    question: 'What plumbing services do you provide?',
    answer:
      'Core work includes emergency plumbing, drain cleaning, water heaters, sewer line repair, leak detection, fixture repairs, and related residential and commercial plumbing. Browse the <a href="/plumbing-services/">full service list</a>.',
  },
  {
    question: 'How much does a plumber cost in Virginia Beach?',
    answer:
      'It depends on urgency, access, parts, and what we find on site. The <a href="/resources/plumber-cost-virginia-beach/">cost factors</a> guide explains what changes the number. Call or request a visit for a quote that matches the actual job.',
  },
  {
    question: 'Do you work with commercial properties?',
    answer:
      'Yes. We work with businesses, property managers, restaurants, offices, and hospitality. Start on the <a href="/commercial-plumbing/">commercial plumbing</a> page.',
  },
  {
    question: 'Do you service rental properties?',
    answer:
      'Yes. Homeowners, landlords, and property owners can request residential plumbing help. See <a href="/residential-plumbing/">residential plumbing</a>.',
  },
  {
    question: 'What should I do during a plumbing emergency?',
    answer:
      'If it is safe, shut off the water at the main valve, keep electricity away from standing water, and call us. Follow the steps on our <a href="/resources/emergency-plumbing-checklist/">emergency plumbing checklist</a>.',
  },
  {
    question: 'How do I request service?',
    answer: `Call <a href="${phone}">${siteConfig.phoneDisplay}</a> anytime, or submit a short request on the <a href="/contact/">contact</a> page.`,
  },
  {
    question: 'What areas do you serve?',
    answer: `We serve Virginia Beach, Virginia, including areas such as Oceanfront, Kempsville, Lynnhaven, Town Center, and Princess Anne. Confirm coverage on the <a href="/service-areas/">service area</a> page.`,
  },
  {
    question: 'Can I request service online?',
    answer: `Yes. Use the request form on this page or on our <a href="/contact/">contact</a> page. If online requests are unavailable, call ${siteConfig.phoneDisplay}. We are open 24/7 for emergency plumbing help.`,
  },
];

const faqSchema = withContext(faqPage(faqs));

const trustItems: Array<{ icon: IconName; title: string; copy: string }> = [
  {
    icon: 'clock',
    title: '24/7 Emergency Service',
    copy: 'Burst pipes, major leaks, and sewer backups.',
  },
  {
    icon: 'home',
    title: 'Residential & Commercial',
    copy: 'Homes, rentals, restaurants, and property managers.',
  },
  {
    icon: 'map',
    title: 'Virginia Beach Local',
    copy: 'We come to the property. Confirm your ZIP.',
  },
];

if (siteConfig.licenseDisplay) {
  trustItems.push({
    icon: 'shield',
    title: siteConfig.licenseDisplay,
    copy: 'License details available when you call.',
  });
} else if (siteConfig.insuranceDisplay) {
  trustItems.push({
    icon: 'shield',
    title: siteConfig.insuranceDisplay,
    copy: 'Coverage details available when you call.',
  });
} else {
  trustItems.push({
    icon: 'check',
    title: 'We Come To You',
    copy: 'No storefront listed. Open 24/7.',
  });
}

const typicalJobs = [
  {
    title: 'Emergency leaks and backups',
    copy: 'Burst pipes, major leaks, and sewage in living spaces. Call first so we can talk through shutoff and next steps.',
    source: 'Typical emergency requests',
  },
  {
    title: 'Drains that will not stay clear',
    copy: 'Kitchens, showers, and main lines that keep clogging. We start with the symptom, then the right clearing method.',
    source: 'Typical drain calls',
  },
  {
    title: 'Water heaters that quit',
    copy: 'No hot water, leaking tanks, and tankless issues. A tank dumping water is urgent; a cold tank is usually scheduled.',
    source: 'Typical water heater calls',
  },
  {
    title: 'Homes, rentals, and businesses',
    copy: 'Landlords, restaurants, and property managers call with fixture failures, backups, and after-hours guest issues.',
    source: 'Typical property calls',
  },
];
---

<BaseLayout
  title={title}
  description={description}
  canonicalPath="/"
  ogImage="/images/hero-plumbing.jpg"
  ogImageAlt="Plumber working on copper supply lines under a kitchen sink (illustrative photo)"
  lcpImage="/images/hero-plumbing.jpg"
  schemaJson={[orgSchema, websiteSchema, webPageSchema, faqSchema]}
>
  <section class="home-hero" aria-label="Virginia Beach plumbing hero">
    <div class="home-hero__media">
      <img
        src="/images/hero-plumbing.jpg"
        alt="Plumber working on copper supply lines under a kitchen sink (illustrative photo)"
        width="1200"
        height="900"
        sizes="100vw"
        fetchpriority="high"
        decoding="async"
      />
    </div>
    <div class="home-hero__inner">
      <div class="home-hero__copy">
        <p class="hero-eyebrow">
          <span class="hero-badge">Virginia Beach</span>
          <span class="hero-badge">Residential &amp; Commercial</span>
        </p>
        <h1>Plumber in<br /><span class="hero-accent">Virginia Beach</span></h1>
        <p class="hero-lead">
          Emergency leaks, clogged drains, water heaters, sewer work, and fixture repairs for homes and businesses in Virginia Beach. Call or send a short request with your ZIP and the problem.
        </p>
        <div class="btn-group hero-ctas">
          <a class="btn btn--primary btn--hero" href={phone} data-track="phone_click" data-track-location="home_hero">
            <Icon name="phone" />
            Call {siteConfig.phoneDisplay}
          </a>
          <a class="btn btn--secondary btn--hero" href="#request-form" data-track="cta_click" data-track-label="home_hero_request">
            Request Service
          </a>
        </div>
        <p class="hero-note">Open 24/7 for emergency plumbing help</p>
      </div>
      <div class="hero-status">
        <p class="hero-status__kicker">24/7 Emergency</p>
        <p class="hero-status__place">Virginia Beach</p>
        <a class="hero-status__call" href={phone} data-track="phone_click" data-track-location="home_hero_card">
          Call for help
        </a>
      </div>
    </div>
  </section>

  <section class="trust-strip" aria-label="Virginia Beach Plumbing facts">
    <div class="container">
      <ul class="trust-strip__row">
        {
          trustItems.map((item) => (
            <li>
              <span class="trust-strip__icon" aria-hidden="true">
                <Icon name={item.icon} size={18} />
              </span>
              <span>
                <strong>{item.title}</strong>
                <span>{item.copy}</span>
              </span>
            </li>
          ))
        }
      </ul>
    </div>
  </section>

  <section class="section home-services">
    <div class="container">
      <div class="section-intro" data-reveal>
        <p class="eyebrow">Services</p>
        <h2>Plumbing work we handle in Virginia Beach</h2>
        <p class="lead">
          Start with the problem. These are the calls we get most. Each page explains signs, what to expect, and how to request help.
        </p>
      </div>
      <ul class="featured-grid">
        {
          featured.map((s, i) => (
            <li data-reveal style={`--d:${i * 70}ms`}>
              <a class="featured-card" href={s.href}>
                <span class="featured-card__icon">
                  <Icon name={s.icon} />
                </span>
                <h3>{s.title}</h3>
                <p>{s.description}</p>
                <span class="card-cta">
                  {s.linkLabel || serviceCtaLabel(s.title)}
                  <Icon name="arrow" />
                </span>
              </a>
            </li>
          ))
        }
      </ul>
      <details class="all-services" data-reveal>
        <summary>All services</summary>
        <ul>
          {
            moreServices.map((s) => (
              <li>
                <a href={s.href}>
                  <span>{s.title}</span>
                  <Icon name="arrow" />
                </a>
              </li>
            ))
          }
          <li>
            <a href="/plumbing-services/">
              <span>View all plumbing services</span>
              <Icon name="arrow" />
            </a>
          </li>
        </ul>
      </details>
    </div>
  </section>

  <section class="section section--emergency home-emergency">
    <div class="container home-emergency__grid">
      <div class="home-emergency__copy" data-reveal>
        <p class="eyebrow">Emergency</p>
        <h2>When Plumbing Can’t Wait</h2>
        <p>
          Burst pipes, major leaks, and sewer backups cannot wait. A water heater dumping water on the floor is urgent; a tank that simply will not heat is usually a scheduled visit. Every emergency is different. Call and we will give you a clear timeline based on your situation.
        </p>
        <div class="btn-group">
          <a class="btn btn--emergency" href={phone} data-track="emergency_call" data-track-location="home">
            <Icon name="phone" />
            Call {siteConfig.phoneDisplay}
          </a>
          <a class="btn btn--ghost" href="/emergency-plumber/" data-track="cta_click" data-track-label="home_emergency">
            Emergency plumbing details
          </a>
        </div>
      </div>
      <div class="home-emergency__media" data-reveal>
        <img
          src="/images/emergency-plumbing.jpg"
          alt="Burst copper pipe spraying water (illustrative photo)"
          width="960"
          height="720"
          loading="lazy"
          decoding="async"
        />
      </div>
    </div>
  </section>

  <section class="home-split" aria-label="Residential and commercial plumbing">
    <a class="split-panel" href="/residential-plumbing/" data-reveal>
      <img
        src="/images/residential-plumbing.jpg"
        alt="Residential bathroom vanity and faucet (illustrative photo)"
        width="1200"
        height="900"
        loading="lazy"
        decoding="async"
      />
      <div class="split-panel__content">
        <p class="eyebrow">Residential</p>
        <h2>Homes &amp; rentals</h2>
        <p>Homeowners, landlords, toilets, drains, water heaters, leaks, and fixture repairs.</p>
        <span class="split-panel__cta">Residential Services</span>
      </div>
    </a>
    <a class="split-panel" href="/commercial-plumbing/" data-reveal>
      <img
        src="/images/commercial-plumbing.jpg"
        alt="Commercial utility room with meters, valves, and copper piping (illustrative photo)"
        width="1200"
        height="900"
        loading="lazy"
        decoding="async"
      />
      <div class="split-panel__content">
        <p class="eyebrow">Commercial</p>
        <h2>Businesses</h2>
        <p>Restaurants, offices, hospitality, and property managers — practical commercial plumbing support.</p>
        <span class="split-panel__cta">Commercial Services</span>
      </div>
    </a>
  </section>

  <section class="section home-why">
    <div class="container home-why__grid">
      <div data-reveal>
        <p class="eyebrow">Why choose us</p>
        <h2>Clear work. Straight answers.</h2>
        <p class="lead">
          When something breaks, you need a useful next step. Share the ZIP and what is going wrong. We will say whether it looks urgent, what to shut off if it is safe, and how to request a visit.
        </p>
      </div>
      <ol class="principle-list">
        {
          principles.map((item, i) => (
            <li data-reveal>
              <span class="principle-list__num">{String(i + 1).padStart(2, '0')}</span>
              <div>
                <h3>{item.title}</h3>
                <p>{item.copy}</p>
              </div>
            </li>
          ))
        }
      </ol>
    </div>
  </section>

  <section class="section section--surface home-process">
    <div class="container">
      <div class="section-intro" data-reveal>
        <p class="eyebrow">How it works</p>
        <h2>Three steps. No runaround.</h2>
      </div>
      <ol class="process-grid">
        {
          steps.map((step) => (
            <li data-reveal>
              <span class="process-grid__num">{step.num}</span>
              <h3>{step.title}</h3>
              <p>{step.copy}</p>
            </li>
          ))
        }
      </ol>
    </div>
  </section>

  <section class="section home-reviews" aria-labelledby="home-reviews-heading">
    <div class="container">
      <div class="section-intro" data-reveal>
        <p class="eyebrow">From the jobs we handle</p>
        <h2 id="home-reviews-heading">What people in Virginia Beach call about</h2>
        <p class="lead">
          These are typical plumbing jobs we handle for homes and businesses here. They are not published Google or third-party reviews, and we do not invent customer names or star ratings.
        </p>
      </div>
      <ul class="review-grid">
        {
          typicalJobs.map((job) => (
            <li data-reveal>
              <article class="review-card">
                <h3>{job.title}</h3>
                <p>{job.copy}</p>
                <p class="review-card__source">{job.source}</p>
              </article>
            </li>
          ))
        }
      </ul>
    </div>
  </section>

  <section class="section home-local">
    <div class="container home-local__grid">
      <div class="home-local__media" data-reveal>
        <img
          src="/images/home-virginia-beach.jpg"
          alt="Wooden boardwalk through coastal dunes at Virginia Beach at dusk"
          width="1400"
          height="900"
          loading="lazy"
          decoding="async"
        />
      </div>
      <div class="home-local__copy" data-reveal>
        <p class="eyebrow">Service area</p>
        <h2>Plumbing help across Virginia Beach</h2>
        <p>
          We work with homeowners, landlords, businesses, and property managers in Virginia Beach. Coastal exposure, older housing stock, and storm-season drainage issues show up often here. Neighborhoods below are common call areas, not separate landing pages.
        </p>
        <ul class="place-list">
          {siteConfig.neighborhoods.map((place) => <li>{place}</li>)}
        </ul>
        <a class="text-link" href="/service-areas/">View service area</a>
      </div>
    </div>
  </section>

  <section class="section section--surface home-guides">
    <div class="container">
      <div class="section-intro" data-reveal>
        <p class="eyebrow">Guides</p>
        <h2>Read this before you guess</h2>
        <p class="lead">
          Short pages for the questions people ask while water is still running, or while they decide whether to call.
        </p>
      </div>
      <ul class="guide-grid">
        {
          homeGuides.map((g) => (
            <li data-reveal>
              <a href={g.href}>
                <span class="guide-kicker">{g.category}</span>
                <h3>{g.title}</h3>
                <p>{g.copy}</p>
                <span class="card-cta">
                  Read guide
                  <Icon name="arrow" />
                </span>
              </a>
            </li>
          ))
        }
      </ul>
      <p class="section-more">
        <a class="text-link" href="/resources/">All plumbing guides</a>
      </p>
    </div>
  </section>

  <section class="section home-contact" id="request-form">
    <div class="container home-form">
      <div class="home-form__intro" data-reveal>
        <p class="eyebrow">Request service</p>
        <h2>Need a plumber in Virginia Beach?</h2>
        <p>
          Share your name, phone, ZIP, and the plumbing problem. For flooding, burst pipes, or sewer backups, calling is faster.
        </p>
        <p class="home-form__phone">
          <a href={phone} data-track="phone_click" data-track-location="home_form">{siteConfig.phoneDisplay}</a>
        </p>
        <p class="home-form__hours">{siteConfig.hoursDisplay} · Serving Virginia Beach, VA</p>
        <p class="home-form__prefer">Prefer to call? For burst pipes, major leaks, or sewer backups, calling is faster.</p>
        <a class="btn btn--primary" href={phone} data-track="phone_click" data-track-location="home_form_btn">
          <Icon name="phone" />
          Call {siteConfig.phoneDisplay}
        </a>
      </div>
      <LeadForm variant="routine" heading="Request service" formId="home-lead-form" />
    </div>
  </section>

  <FaqList items={faqs} heading="Frequently asked questions" />

  <section class="section section--primary home-final">
    <div class="container home-final__inner" data-reveal>
      <p class="eyebrow eyebrow--on-dark">Virginia Beach, VA</p>
      <h2>Need a plumber in Virginia Beach?</h2>
      <p>
        Call or send a short request with your ZIP and the problem you’re dealing with.
      </p>
      <div class="btn-group">
        <a class="btn btn--on-dark" href={phone} data-track="phone_click" data-track-location="home_final">
          <Icon name="phone" />
          Call {siteConfig.phoneDisplay}
        </a>
        <a class="btn btn--ghost" href="#request-form" data-track="cta_click" data-track-label="home_final_request">
          Request Service
        </a>
      </div>
    </div>
  </section>
</BaseLayout>

<style>
  .home-hero {
    position: relative;
    display: flex;
    align-items: stretch;
    isolation: isolate;
    overflow: hidden;
    min-height: min(42rem, calc(100svh - 5.5rem));
    color: #fff;
    background: var(--color-navy);
  }

  .home-hero__media {
    position: absolute;
    inset: 0;
    z-index: 0;
    margin: 0;
    overflow: hidden;
    border: 0;
    border-radius: 0;
    background: var(--color-navy-deep);
    box-shadow: none;
  }

  .home-hero__media img {
    width: 100%;
    height: 100%;
    min-height: 22rem;
    object-fit: cover;
    object-position: 62% 40%;
  }

  .home-hero__media::after {
    content: '';
    position: absolute;
    inset: 0;
    pointer-events: none;
    background:
      linear-gradient(180deg, rgba(7, 24, 46, 0.55) 0%, rgba(11, 37, 69, 0.42) 36%, rgba(7, 24, 46, 0.88) 100%),
      linear-gradient(90deg, rgba(11, 37, 69, 0.94) 0%, rgba(11, 37, 69, 0.78) 42%, rgba(11, 37, 69, 0.38) 100%);
  }

  .home-hero__inner {
    position: relative;
    z-index: 1;
    display: grid;
    gap: 1.5rem;
    align-content: end;
    width: min(100% - 2rem, var(--container));
    margin: 0 auto;
    padding: 2.25rem 0 2.5rem;
  }

  .home-hero__copy {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    color: #fff;
    padding: 0;
  }

  .hero-eyebrow {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
    margin: 0 0 1.15rem;
  }

  .hero-badge {
    display: inline-flex;
    align-items: center;
    min-height: 2rem;
    padding: 0.35rem 0.85rem;
    border: 1px solid rgba(255, 255, 255, 0.28);
    border-radius: 999px;
    background: rgba(255, 255, 255, 0.12);
    box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.12);
    backdrop-filter: blur(10px);
    color: #fff;
    font-family: var(--font-heading);
    font-size: 0.78rem;
    font-weight: 650;
    letter-spacing: 0.04em;
    text-transform: uppercase;
  }

  .home-hero h1 {
    margin: 0 0 1.05rem;
    color: #fff;
    font-size: clamp(2.55rem, 1.2rem + 5vw, 5rem);
    font-weight: 800;
    line-height: 0.94;
    letter-spacing: -0.048em;
    text-wrap: balance;
  }

  .hero-accent {
    color: #fff;
    background: none;
    -webkit-background-clip: unset;
    background-clip: unset;
  }

  .hero-lead {
    margin: 0 0 1.6rem;
    max-width: 36rem;
    color: rgba(255, 255, 255, 0.9);
    font-size: clamp(1.05rem, 0.98rem + 0.3vw, 1.2rem);
    line-height: 1.6;
  }

  .hero-ctas {
    margin: 0 0 1rem;
    gap: 0.75rem;
  }

  .btn--hero {
    min-height: 52px;
    padding-inline: 1.35rem;
    font-weight: 700;
  }

  .home-hero .btn--primary {
    background: var(--color-amber);
    border-color: var(--color-amber);
    color: #fff;
    box-shadow: 0 10px 24px rgba(194, 65, 12, 0.35);
  }

  .home-hero .btn--primary:hover,
  .home-hero .btn--primary:focus-visible {
    background: var(--color-amber-hover);
    border-color: var(--color-amber-hover);
    color: #fff;
  }

  .home-hero .btn--secondary {
    background: rgba(255, 255, 255, 0.1);
    border-color: rgba(255, 255, 255, 0.55);
    color: #fff;
    box-shadow: none;
  }

  .home-hero .btn--secondary:hover,
  .home-hero .btn--secondary:focus-visible {
    background: #fff;
    border-color: #fff;
    color: var(--color-navy);
  }

  .hero-note {
    margin: 0;
    color: rgba(255, 255, 255, 0.78);
    font-size: 0.92rem;
  }

  .hero-status {
    justify-self: start;
    width: min(100%, 18.5rem);
    padding: 1.05rem 1.1rem 1.15rem;
    border: 1px solid rgba(255, 255, 255, 0.18);
    border-radius: 16px;
    background: rgba(7, 24, 46, 0.62);
    box-shadow: 0 18px 40px rgba(0, 0, 0, 0.28);
    backdrop-filter: blur(16px);
  }

  .hero-status__kicker {
    display: inline-flex;
    align-items: center;
    margin: 0 0 0.55rem;
    padding: 0.22rem 0.65rem;
    border-radius: 999px;
    background: rgba(194, 65, 12, 0.18);
    border: 1px solid rgba(253, 186, 116, 0.45);
    color: #ffedd5;
    font-family: var(--font-heading);
    font-size: 0.68rem;
    font-weight: 700;
    letter-spacing: 0.12em;
    text-transform: uppercase;
  }

  .hero-status__place {
    margin: 0 0 0.75rem;
    color: #fff;
    font-size: 1.2rem;
    font-weight: 800;
    letter-spacing: -0.03em;
  }

  .hero-status__call {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-height: 42px;
    padding: 0.45rem 0.9rem;
    border-radius: 10px;
    background: var(--color-amber);
    color: #fff;
    font-weight: 700;
    font-size: 0.9rem;
    text-decoration: none;
  }

  .hero-status__call:hover,
  .hero-status__call:focus-visible {
    background: var(--color-amber-hover);
    color: #fff;
  }

  .home-hero .btn:focus-visible,
  .hero-status__call:focus-visible {
    outline: 2px solid #fff;
    outline-offset: 3px;
  }

  .trust-strip {
    position: relative;
    z-index: 2;
    margin-top: -1px;
    padding: 0;
    background: #fff;
    border-bottom: 1px solid var(--color-border);
    box-shadow: 0 12px 28px rgba(15, 35, 70, 0.06);
  }

  .trust-strip__row {
    list-style: none;
    margin: 0;
    padding: 0.85rem 0;
    display: grid;
    gap: 0;
    grid-template-columns: 1fr;
  }

  .trust-strip__row li {
    display: grid;
    grid-template-columns: 1.85rem minmax(0, 1fr);
    gap: 0.7rem;
    align-items: start;
    padding: 0.7rem 0;
    border-top: 1px solid var(--color-border);
  }

  .trust-strip__row li:first-child {
    border-top: 0;
    padding-top: 0.15rem;
  }

  .trust-strip__icon {
    display: flex;
    width: 1.85rem;
    height: 1.85rem;
    align-items: center;
    justify-content: center;
    color: var(--color-accent-strong);
  }

  .trust-strip strong {
    display: block;
    font-family: var(--font-heading);
    font-size: 0.9rem;
    font-weight: 700;
    letter-spacing: -0.02em;
    color: var(--color-text);
    margin-bottom: 0.12rem;
  }

  .trust-strip span span {
    display: block;
    font-size: 0.84rem;
    line-height: 1.4;
    color: var(--color-muted);
  }

  .section-intro {
    max-width: 40rem;
    margin-bottom: var(--space-6);
  }

  .featured-grid {
    list-style: none;
    margin: 0;
    padding: 0;
    display: grid;
    gap: 1.15rem;
  }

  .featured-card {
    display: flex;
    flex-direction: column;
    height: 100%;
    padding: 1.4rem 1.3rem 1.25rem;
    background: #fff;
    border: 1px solid var(--color-border);
    border-radius: var(--radius-card);
    text-decoration: none;
    color: inherit;
    box-shadow: var(--shadow-sm);
    transition:
      border-color 0.2s ease,
      transform 0.2s ease,
      box-shadow 0.2s ease,
      background-color 0.2s ease;
  }

  .featured-card:hover,
  .featured-card:focus-visible {
    color: inherit;
    text-decoration: none;
    border-color: rgba(11, 37, 69, 0.28);
    background: var(--color-surface-hover);
    box-shadow: var(--shadow-glow);
    transform: translateY(-4px);
  }

  .featured-card__icon {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 44px;
    height: 44px;
    margin-bottom: 0.95rem;
    border-radius: 12px;
    background: var(--color-primary-soft);
    color: var(--color-navy);
    transition: transform 0.2s ease;
  }

  .featured-card:hover .featured-card__icon {
    transform: translateY(-2px);
  }

  .featured-card h3 {
    margin: 0 0 0.45rem;
    font-size: 1.2rem;
    letter-spacing: -0.02em;
  }

  .featured-card p {
    margin: 0 0 1rem;
    color: var(--color-muted);
    font-size: 0.96rem;
    flex: 1;
  }

  .featured-card .card-cta {
    display: inline-flex;
    align-items: center;
    gap: 0.35rem;
    font-family: var(--font-heading);
    font-weight: 650;
    font-size: 0.88rem;
    color: var(--color-accent);
    letter-spacing: 0.01em;
    text-transform: none;
  }

  .featured-card:hover .card-cta {
    color: var(--color-accent-strong);
  }

  .text-link {
    font-family: var(--font-heading);
    font-weight: 650;
    color: var(--color-accent-strong);
    text-decoration: none;
  }

  .text-link:hover {
    color: var(--color-navy);
  }

  .all-services {
    margin-top: var(--space-6);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-card);
    background: var(--color-surface);
    padding: 0.35rem 1.1rem 0.55rem;
  }

  .all-services summary {
    cursor: pointer;
    list-style: none;
    min-height: 52px;
    display: flex;
    align-items: center;
    justify-content: space-between;
    font-weight: 650;
    font-family: var(--font-heading);
  }

  .all-services summary::-webkit-details-marker {
    display: none;
  }

  .all-services summary::after {
    content: '';
    width: 0.45rem;
    height: 0.45rem;
    border-right: 1.5px solid currentColor;
    border-bottom: 1.5px solid currentColor;
    transform: rotate(45deg);
    transition: transform 0.18s ease;
  }

  .all-services[open] summary::after {
    transform: rotate(225deg);
  }

  .all-services ul {
    list-style: none;
    margin: 0 0 0.7rem;
    padding: 0;
    display: grid;
    gap: 0.35rem;
  }

  .all-services a {
    display: flex;
    align-items: center;
    justify-content: space-between;
    min-height: 44px;
    padding: 0.35rem 0.1rem;
    color: var(--color-text);
    text-decoration: none;
    border-bottom: 1px solid var(--color-border);
  }

  .all-services a:hover {
    color: var(--color-accent-strong);
  }

  .home-emergency__grid {
    display: grid;
    gap: var(--space-6);
    align-items: center;
  }

  .home-emergency__copy p:not(.eyebrow) {
    max-width: 38rem;
  }

  .home-emergency__media img {
    width: 100%;
    aspect-ratio: 4 / 3;
    object-fit: cover;
    object-position: center;
    border-radius: var(--radius-card);
    border: 1px solid rgba(255, 255, 255, 0.16);
  }

  .home-split {
    display: grid;
    gap: 0.75rem;
    padding: 0 1rem 0;
    max-width: calc(var(--container) + 2rem);
    margin-inline: auto;
  }

  .split-panel {
    position: relative;
    min-height: 24rem;
    overflow: hidden;
    color: var(--color-text);
    text-decoration: none;
    isolation: isolate;
    border-radius: var(--radius-card);
    border: 1px solid var(--color-border);
    background: #fff;
    box-shadow: var(--shadow-sm);
  }

  .split-panel:hover {
    color: var(--color-text);
    text-decoration: none;
  }

  .split-panel img {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.7s ease;
  }

  .split-panel:hover img {
    transform: scale(1.04);
  }

  .split-panel::after {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(180deg, rgba(247, 249, 252, 0.04) 0%, rgba(11, 18, 32, 0.28) 100%);
    z-index: 1;
    transition: background 0.25s ease;
  }

  .split-panel:hover::after {
    background: linear-gradient(180deg, rgba(247, 249, 252, 0.02) 0%, rgba(11, 18, 32, 0.18) 100%),
      radial-gradient(ellipse at 50% 80%, rgba(11, 37, 69, 0.2), transparent 55%);
  }

  .split-panel__content {
    position: relative;
    z-index: 2;
    min-height: 26rem;
    display: flex;
    flex-direction: column;
    justify-content: flex-end;
    padding: 1.2rem;
    background: linear-gradient(180deg, transparent 0%, rgba(255, 255, 255, 0.08) 38%, rgba(255, 255, 255, 0.96) 68%);
  }

  .split-panel h2 {
    color: var(--color-text);
    margin-bottom: 0.6rem;
    font-size: clamp(1.7rem, 1.4rem + 1.2vw, 2.4rem);
    letter-spacing: -0.03em;
  }

  .split-panel p {
    color: var(--color-text-secondary);
    max-width: 34ch;
    margin-bottom: 1.1rem;
  }

  .split-panel__cta {
    font-family: var(--font-heading);
    font-weight: 700;
    font-size: 0.88rem;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--color-navy);
  }

  .home-why__grid {
    display: grid;
    gap: var(--space-7);
  }

  .principle-list {
    list-style: none;
    margin: 0;
    padding: 0;
    position: relative;
  }

  .principle-list::before {
    content: '';
    position: absolute;
    left: 1.05rem;
    top: 0.85rem;
    bottom: 0.85rem;
    width: 1px;
    background: rgba(11, 37, 69, 0.16);
  }

  .principle-list li {
    display: grid;
    grid-template-columns: 3.6rem 1fr;
    gap: 1rem;
    padding: 1.15rem 0;
    position: relative;
  }

  .principle-list li:last-child {
    padding-bottom: 0;
  }

  .principle-list__num {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 2.15rem;
    height: 2.15rem;
    border-radius: 999px;
    background: var(--color-navy);
    color: #fff;
    font-family: var(--font-heading);
    font-size: 0.78rem;
    font-weight: 800;
    letter-spacing: 0.04em;
    z-index: 1;
  }

  .principle-list h3 {
    margin: 0 0 0.35rem;
    font-size: 1.18rem;
    letter-spacing: -0.02em;
  }

  .principle-list p {
    margin: 0;
    color: var(--color-muted);
  }

  .process-grid {
    list-style: none;
    margin: 0;
    padding: 0;
    display: grid;
    gap: var(--space-5);
    position: relative;
  }

  .process-grid li {
    background: var(--color-surface);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-card);
    padding: 1.4rem 1.3rem 1.35rem;
    position: relative;
    z-index: 1;
    min-width: 0;
  }

  .process-grid__num {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 2.75rem;
    height: 2.75rem;
    margin-bottom: 0.95rem;
    border-radius: 999px;
    border: 1.5px solid var(--color-navy);
    background: #fff;
    font-family: var(--font-heading);
    font-size: 0.82rem;
    font-weight: 800;
    letter-spacing: 0.04em;
    color: var(--color-navy);
    position: relative;
    z-index: 1;
  }

  .process-grid h3 {
    margin-bottom: 0.5rem;
    font-size: 1.22rem;
    letter-spacing: -0.02em;
  }

  .process-grid p {
    margin: 0;
    color: var(--color-muted);
    max-width: 34ch;
  }

  .home-reviews {
    background: #fff;
  }

  .review-grid {
    list-style: none;
    margin: 0;
    padding: 0;
    display: grid;
    gap: 1rem;
  }

  .review-card {
    height: 100%;
    padding: 1.25rem 1.2rem 1.15rem;
    border: 1px solid var(--color-border);
    border-radius: var(--radius-card);
    background: var(--color-bg);
  }

  .review-card h3 {
    margin: 0 0 0.45rem;
    font-size: 1.08rem;
    letter-spacing: -0.02em;
  }

  .review-card p {
    margin: 0;
    color: var(--color-text-secondary);
    font-size: 0.95rem;
  }

  .review-card__source {
    margin-top: 0.85rem !important;
    color: var(--color-muted) !important;
    font-size: 0.78rem !important;
    font-weight: 650;
    letter-spacing: 0.04em;
    text-transform: uppercase;
  }

  .home-local__grid {
    display: grid;
    gap: var(--space-6);
    align-items: center;
  }

  .home-local__media {
    position: relative;
    isolation: isolate;
  }

  .home-local__media::after {
    content: '';
    position: absolute;
    inset: 0;
    border-radius: var(--radius-card);
    pointer-events: none;
    background: none;
  }

  .home-local__media img {
    width: 100%;
    aspect-ratio: 4 / 3;
    object-fit: cover;
    object-position: center;
    border-radius: var(--radius-card);
    border: 1px solid var(--color-border);
  }

  .place-list {
    list-style: none;
    margin: 1.25rem 0 1.4rem;
    padding: 0;
    display: flex;
    flex-wrap: wrap;
    gap: 0.45rem;
  }

  .place-list li {
    font-size: 0.8rem;
    font-weight: 650;
    letter-spacing: 0.02em;
    color: var(--color-navy);
    background: #fff;
    border: 1px solid var(--color-border);
    padding: 0.42rem 0.8rem;
    border-radius: 999px;
  }

  .home-final__inner {
    max-width: 42rem;
  }

  .home-final h2 {
    font-size: clamp(2rem, 1.2rem + 2.2vw, 3.2rem);
    letter-spacing: -0.04em;
  }

  .home-contact {
    background: transparent;
  }

  .home-form {
    display: grid;
    gap: var(--space-6);
    align-items: start;
  }

  .home-form__intro {
    background: #fff;
    color: var(--color-text);
    padding: 1.75rem 1.5rem;
    border-radius: var(--radius-card);
    border: 1px solid var(--color-border);
    box-shadow: var(--shadow-sm);
  }

  .home-form__intro h2 {
    color: var(--color-text);
    letter-spacing: -0.03em;
  }

  .home-form__phone {
    margin: 1.1rem 0 0.25rem;
  }

  .home-form__phone a {
    font-family: var(--font-heading);
    font-size: clamp(1.5rem, 3vw, 1.9rem);
    font-weight: 700;
    color: var(--color-amber);
    letter-spacing: -0.03em;
    text-decoration: none;
  }

  .home-form__phone a:hover {
    color: var(--color-amber-hover);
  }

  .home-form__hours,
  .home-form__prefer {
    margin-bottom: 1rem;
    font-size: 0.95rem;
  }

  .guide-grid {
    list-style: none;
    margin: 0;
    padding: 0;
    display: grid;
    gap: 1rem;
  }

  .guide-grid a {
    display: flex;
    flex-direction: column;
    height: 100%;
    padding: 1.25rem 1.2rem;
    background: #fff;
    border: 1px solid var(--color-border);
    border-radius: var(--radius-card);
    text-decoration: none;
    color: inherit;
    box-shadow: var(--shadow-sm);
    transition:
      border-color 0.2s ease,
      transform 0.2s ease;
  }

  .guide-grid a:hover,
  .guide-grid a:focus-visible {
    border-color: rgba(11, 37, 69, 0.28);
    color: inherit;
    text-decoration: none;
    transform: translateY(-3px);
  }

  .guide-kicker {
    font-family: var(--font-mono);
    font-size: 0.68rem;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    color: var(--color-accent-strong);
    margin-bottom: 0.55rem;
  }

  .guide-grid h3 {
    margin: 0 0 0.35rem;
    font-size: 1.18rem;
  }

  .guide-grid p {
    margin: 0 0 1rem;
    color: var(--color-muted);
    font-size: 0.95rem;
    flex: 1;
  }

  .guide-grid .card-cta {
    display: inline-flex;
    align-items: center;
    gap: 0.3rem;
  }

  @media (min-width: 700px) {
    .guide-grid {
      grid-template-columns: 1fr 1fr;
    }

    .review-grid {
      grid-template-columns: 1fr 1fr;
    }

    .trust-strip__row {
      grid-template-columns: 1fr 1fr;
      gap: 0 1.25rem;
      padding: 1rem 0;
    }

    .trust-strip__row li {
      border-top: 0;
      padding: 0.35rem 0.75rem 0.35rem 0;
    }

    .trust-strip__row li:nth-child(odd) {
      border-right: 1px solid var(--color-border);
      padding-right: 1rem;
    }

    .featured-grid {
      grid-template-columns: 1fr 1fr;
    }

    .home-hero__media img {
      min-height: 100%;
    }

    .hero-status {
      justify-self: end;
      align-self: end;
    }

    .home-split {
      grid-template-columns: 1fr 1fr;
    }

    .all-services ul {
      grid-template-columns: 1fr 1fr;
      gap: 0.35rem 1.25rem;
    }
  }

  @media (min-width: 900px) {
    .home-form {
      grid-template-columns: 0.9fr 1.1fr;
      gap: var(--space-7);
    }

    .home-form :global(.form) {
      min-height: 28rem;
    }

    .home-form__intro {
      padding: 2.1rem 1.85rem;
    }
  }

  @media (min-width: 980px) {
    .home-hero {
      min-height: min(46rem, calc(100svh - 5.75rem));
    }

    .home-hero__inner {
      grid-template-columns: minmax(0, 1.15fr) minmax(16rem, 0.55fr);
      align-items: end;
      gap: 2rem;
      padding: 4.25rem 0 3.5rem;
    }

    .home-hero__media::after {
      background:
        linear-gradient(180deg, rgba(7, 24, 46, 0.28) 0%, rgba(11, 37, 69, 0.18) 40%, rgba(7, 24, 46, 0.72) 100%),
        linear-gradient(90deg, rgba(11, 37, 69, 0.92) 0%, rgba(11, 37, 69, 0.72) 36%, rgba(11, 37, 69, 0.28) 72%, rgba(11, 37, 69, 0.18) 100%);
    }

    .home-hero__media,
    .home-hero__media img {
      min-height: 100%;
      max-height: none;
      height: 100%;
    }

    .trust-strip__row {
      grid-template-columns: repeat(4, minmax(0, 1fr));
      align-items: start;
      padding: 0.95rem 0;
    }

    .trust-strip__row li {
      padding: 0 1rem 0 0;
      border-right: 1px solid var(--color-border);
    }

    .trust-strip__row li:nth-child(odd) {
      border-right: 1px solid var(--color-border);
      padding-right: 1rem;
    }

    .trust-strip__row li:last-child {
      border-right: 0;
      padding-right: 0;
    }

    .featured-grid {
      grid-template-columns: repeat(6, 1fr);
    }

    .featured-grid li:nth-child(1) {
      grid-column: span 3;
      grid-row: span 2;
    }

    .featured-grid li:nth-child(2),
    .featured-grid li:nth-child(3) {
      grid-column: span 3;
    }

    .featured-grid li:nth-child(4),
    .featured-grid li:nth-child(5),
    .featured-grid li:nth-child(6) {
      grid-column: span 2;
    }

    .featured-grid li:nth-child(1) .featured-card {
      min-height: 100%;
      justify-content: flex-end;
      padding: 1.8rem 1.6rem 1.5rem;
    }

    .featured-grid li:nth-child(1) h3 {
      font-size: 1.7rem;
    }

    .process-grid {
      grid-template-columns: repeat(3, 1fr);
    }

    .process-grid::before {
      content: '';
      position: absolute;
      top: 2.7rem;
      left: 10%;
      right: 10%;
      height: 1px;
      background: rgba(11, 37, 69, 0.16);
    }

    .review-grid {
      grid-template-columns: 1fr 1fr;
    }

    .home-emergency__grid,
    .home-why__grid,
    .home-local__grid {
      grid-template-columns: 1fr 1fr;
      gap: var(--space-8);
    }

    .home-emergency__media img,
    .home-local__media img {
      min-height: 100%;
      aspect-ratio: 5 / 4;
    }

    .split-panel,
    .split-panel__content {
      min-height: 32rem;
    }

    .split-panel__content {
      padding: 2.4rem;
    }

    .guide-grid {
      grid-template-columns: repeat(6, 1fr);
    }

    .guide-grid li:nth-child(1),
    .guide-grid li:nth-child(2) {
      grid-column: span 3;
    }

    .guide-grid li:nth-child(3),
    .guide-grid li:nth-child(4),
    .guide-grid li:nth-child(5) {
      grid-column: span 2;
    }
  }

  @media (max-width: 599px) {
    .hero-ctas {
      display: grid;
      grid-template-columns: 1fr;
      width: 100%;
    }

    .hero-ctas .btn {
      width: 100%;
    }

    .home-hero {
      min-height: 34rem;
    }

    .home-hero h1 {
      max-width: none;
      font-size: clamp(2.35rem, 9vw, 2.85rem);
    }

    .home-hero__inner {
      padding: 1.75rem 0 1.85rem;
    }

    .hero-status {
      width: 100%;
    }

    .hero-status__place {
      font-size: 1.05rem;
    }

    .hero-status__call {
      width: 100%;
    }

    .trust-strip__row li:nth-child(odd) {
      border-right: 0;
      padding-right: 0;
    }
  }

  @media (max-width: 375px) {
    .home-hero {
      min-height: 32rem;
    }

    .home-hero__inner {
      width: min(100% - 1.5rem, var(--container));
      padding: 1.45rem 0 1.5rem;
    }

    .hero-badge {
      font-size: 0.7rem;
      padding: 0.28rem 0.7rem;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .home-hero .btn,
    .hero-status__call {
      transition: none;
    }
  }
</style>
